<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<%- include('nav', {user: user}) %>
<body>    
  <% if (user) {%>
    <h1>Welcome <%= user.firstName %> <%= user.lastName %>, type in the secret passcode to join the club</h1>
    <form action="./join" method="POST">
      <label for="passcode">Pass Code:</label>
      <input name="passcode" placeholder="enter the passcode" type="text" />
      <button>Enter</button>
    </form>
    <p>Hint: the passcode is: 123</p>
  <% } else { %>
    <h1>please log in or sign up to join the club</h1>
  <%}%>
</body>
</html>